{
  "name": "blessme-web-ui",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "vite",
    "build": "node scripts/generate-sitemap.js && vite build && node scripts/prerender.js",
    "preview": "vite preview",
    "check:seo": "npm run build && node scripts/check-search-links.js && node scripts/check-seo-foundation.js && node scripts/check-analytics.mjs",
    "check:cursor": "python3 scripts/check-cursor-cleanup.py",
    "check:images": "python3 scripts/check-image-budget.py",
    "check:perf": "npm run build && python3 scripts/check-cursor-cleanup.py && python3 scripts/check-image-budget.py"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.3.2",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.5.2",
    "autoprefixer": "^10.5.2",
    "clsx": "^2.1.1",
    "framer-motion": "^12.42.2",
    "postcss": "^8.5.16",
    "tailwind-merge": "^3.6.0",
    "tailwindcss": "^4.3.2",
    "terser": "^5.47.1",
    "vite": "^6.3.5",
    "wrangler": "^4.108.0"
  }
}
